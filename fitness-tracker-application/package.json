{
  "name": "fitness-tracker-application",
  "version": "0.0.0",
  "description": "FitTrack Pro - Fitness Tracking & Community Application",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev:server --workspace=client",
    "dev:all": "npm run dev:all --workspace=client",
    "build": "npm run build --workspace=client",
    "build:server": "npm run build:server --workspace=client",
    "preview": "npm run preview --workspace=client",
    "start": "npm run start --workspace=client",
    "start:server": "npm run start --workspace=client",
    "lint": "npm run lint --workspace=client",
    "format": "npm run format --workspace=client",
    "seed": "npm run seed --workspace=client"
  },
  "workspaces": [
    "client"
  ],
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "dotenv": "^16.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.11.0",
    "tsx": "^4.7.0",
    "typescript": "~5.9.3"
  }
}
